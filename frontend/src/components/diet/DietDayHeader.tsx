export default function DietDayHeader() {
  return (
    <div class="contents">
      <div class="col-span-2 text-sm font-bold text-gray-400"></div>
      <div class="hidden items-center justify-end p-2 text-sm font-bold text-gray-400 md:flex">
        Quantity
      </div>
      <div class="hidden items-center justify-end p-2 text-sm font-bold text-gray-400 md:flex">
        Calories
      </div>
      <div class="hidden items-center justify-end p-2 text-sm font-bold text-gray-400 md:flex">
        Protein
      </div>
      <div class="hidden items-center justify-end p-2 text-sm font-bold text-gray-400 md:flex">
        Carbs
      </div>
      <div class="hidden items-center justify-end p-2 text-sm font-bold text-gray-400 md:flex">
        Fat
      </div>
      <div class="hidden items-center justify-end p-2 text-sm font-bold text-gray-400 md:flex">
        Sat. Fat
      </div>
      <div class="hidden items-center justify-end p-2 text-sm font-bold text-gray-400 md:flex">
        Sugars
      </div>
      <div class="hidden items-center justify-end p-2 text-sm font-bold text-gray-400 md:flex">
        Fibre
      </div>
      <div class="hidden items-center justify-end p-2 text-sm font-bold text-gray-400 md:flex">
        Salt
      </div>
    </div>
  );
}
